# ==================================================
#        权限模块统一配置（两段式）
#   config  — 静态配置节（晋升阈值等）
#   reviews — 审核记录节（运行时，勿手改）
#   权限组状态由 LuckPerms track 持有，本地不存储
# ==================================================
config:
  # default→member 自动晋升阈值（小时）
  member-threshold-hours: 10

# ---- 以下为运行时状态，由插件自动管理 ----
reviews:
  requests: {}
