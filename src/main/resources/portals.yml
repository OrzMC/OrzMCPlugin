# ==================================================
#             传送门映射（运行时管理）
# ==================================================
# 运行中通过 /portal 命令管理，不建议手动编辑。
# - key 为服务器地址 "host:port"（用 _ 代替 .）
# - value 为传送门中心坐标 "world:cx:cy:cz" -> 轴向 "X"/"Z"
portals: {}
