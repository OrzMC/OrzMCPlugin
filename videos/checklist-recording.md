# 录制清单（按集分节）

> **状态：现行** ｜ **最后更新**：2026-09-13
> 用法：录屏前对照本节「录什么」，录屏后逐条自检。**原始录屏不入库**（放仓库外或用 `VIDEO_WORKDIR` 指到仓库外）。
> 素材命名与分镜镜号一一对应 ⇒ **支持单镜重录替换**（功能迭代后按 `UPDATE.md` 的 L2 处置）。

## 通用要求

| 项 | 要求 |
|:--|:--|
| 分辨率 / 帧率 | 1920×1080 @ 60fps（横屏主片）；9:16 切片另录或后期裁切（安全框见下） |
| 光标 | 放大到 150%、加高亮光晕（录屏软件设定） |
| 录制范围 | 只录必要区域（群聊窗口 / 游戏窗口 / 终端），不出现桌面、通知、其它窗口 |
| 音频 | 先录无声画面（口播/音乐后期配）；如需现场口播，另存干净音轨 |
| 文件命名 | `epNN_MM_topic.mp4`（如 `ep01_04_ops.mp4`），与源文件 `shots[].asset` 完全一致 |
| 单镜时长 | 允许 ≥ 分镜时长，**不要短于**（剪辑时按分镜裁剪） |
| 9:16 安全框 | 上下各留 12% 安全区（避开平台 UI），字幕避开底部 15%；关键操作居中 |
| 录屏目录 | `$VIDEO_WORKDIR/<epNN>/raw/`（默认 `videos/.build/<epNN>/raw/`，已 gitignore）；建议仓库外 |

### ⚠️ 敏感信息遮蔽清单（录制前先清场，录后逐帧复查）

| 类别 | 处理 |
|:--|:--|
| 服务器 IP / 域名 / 端口 | 用合成值（如 `mc.example.com:25565` → 演示环境用 `play.local:25565`）；必要时马赛克 |
| QQ 群号 / 会话 ID / openid | 遮蔽或改备注为「管理群」「玩家群」；`0F1E…E1F0` 形态的合成值可用 |
| Bot Token / API Key / RCON 密码 | **绝不入画**：配置文件里对应行先替换为 `***` 或折叠；终端不回显凭据 |
| 玩家真实昵称 | 用测试账号（如 `Test01`…）或打码；避免真实玩家隐私 |
| 后台面板地址 / 账号 | 不录面板画面；必要时只录控制台命令回显 |

> 提交与分享前再跑一次：`videos/tools/verify.sh`（内含源级敏感信息模式检查）。

---

## EP0 宣传短片（70s，5 镜）

| 镜 | 素材名 | 录什么 | 步骤 | 自检 |
|---:|---|---|---|---|
| 1 | `ep00_01_open.png` | 卡片（脚本生成，无需录屏） | `make.sh ep00 --cards` | 标题与标语无错字；水印可见 |
| 2 | `ep00_02_group_cmds.mp4` | QQ 管理群：`$l` 看在线 → `$a <玩家>` 加白 → `$r <玩家>` 移除 | 先把 2–3 个测试账号加白/移除一次，录「命令 → 机器人回显」完整链路；单条命令间留 1s 静止便于剪辑 | 命令与回显同屏；群号/昵称已遮蔽；无其它会话弹出 |
| 3 | `ep00_03_review.mp4` | 玩家侧 `/apply` 提交 + 管理群 `$v l` 列表 → `$v y` 通过 → 群内晋升通知 | 用 member 组测试号提交 builder 申请；通过后确认通知到达玩家群 | 玩家名走测试账号；申请→通过→通知三段都可辨 |
| 4 | `ep00_04_safety_backup.mp4` | 游戏内白名单拦截提示 + TNT 告警群消息 + `$b` 备份进度推送 | 用未加白账号登录录拦截画面（3–5s）；触发一次 TNT 告警；`$b` 触发后录到进度与完成回传 | 拦截原因文案可见；告警/进度消息完整；不出现真实 IP |
| 5 | `ep00_05_end.png` | 卡片（脚本生成） | `make.sh ep00 --cards` | 下载渠道与二维码占位正确；官网链接无误 |

**9:16 切片**：取镜 2 重剪（`aspect_variants.shot_ids: [2]`，≤45s），字幕字号 ≥48px、单行 ≤10 字。

---

## EP1 群指令总览（145s，6 镜）

| 镜 | 素材名 | 录什么 | 步骤 | 自检 |
|---:|---|---|---|---|
| 1 | `ep01_01_pain.png` | 卡片（脚本生成） | `make.sh ep01 --cards` | 三条提问气泡文案与口播一致 |
| 2 | `ep01_02_cmd_system.mp4` | 群内 `$h` 帮助回显 + `$h ?` 用法回显 | 依次发送两条命令，每次等机器人回显完整再发下一条 | 11 条命令列表可读；回显无截断 |
| 3 | `ep01_03_bind.mp4` | 控制台 `/config im bind <类型>` 与 `/config im status` 已绑定状态 | 在测试服控制台执行；`status` 输出中的会话 ID **必须先遮蔽** | 绑定成功提示可见；无 Token/IP/会话明文 |
| 4 | `ep01_04_ops.mp4` | 管理群实操四连：`$l` → `$a <玩家>` → `$w` → `$r <玩家>` + 玩家被拦截提示 | 用测试号先移除再登录录拦截；四条命令各留 2s 静止 | 四步齐全、顺序正确；被移除者拦截提示同屏 |
| 5 | `ep01_05_notify.mp4` | 群内通知流：上下线 / 白名单拦截 / 备份进度 | 让测试号进出 2 次、`$b` 触发一次，录通知滚动画面（≥15s） | 三类通知都出现；消息样式与 `templates.yml` 一致 |
| 6 | `ep01_06_summary.png` | 卡片（脚本生成） | `make.sh ep01 --cards` | 三条要点与下集预告文案正确 |

**9:16 切片**：取镜 4 重剪（`aspect_variants.shot_ids: [4]`，≤60s）。

---

## 录完后的收尾

1. 素材按命名放入 `$VIDEO_WORKDIR/<epNN>/raw/`（仓库外）；
2. 复核敏感信息清单（逐帧扫一遍群名、ID、IP、Token）；
3. 合成时对齐 `.build/<epNN>/<epNN>.zh-CN.srt` 与分镜时长（口播样音 `preview/*.mp3` 可作配音参考）；
4. 发布后回填 `status.md`（成片链接 + 适用版本），命名按 `EP01-群指令-v1.0.28`；
5. 若后续功能变更命中本集，按 `UPDATE.md` 的 L1/L2/L3 处置（L2 只重录对应镜号即可）。
