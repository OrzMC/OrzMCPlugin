# OrzMC 插件宣传视频 — 制作清单

> **本文件为 AI 视频生成（HyperFrames）准备的生产清单**  
> **视频时长：** 8–10 分钟  
> **目标受众：** PaperMC 服务器服主、管理员、Minecraft 技术玩家  
> **风格：** 功能演示 + 操作实录 + 口播解说  
> **分辨率：** 1920×1080 / 60fps  
> **口播语言：** 中文普通话

---

## 📦 素材清单（Asset Inventory）

所有素材需提前录制/准备到位，按段落编号命名。

```
promo-video/
├── source/                          ← 源素材（原始录制/截图）
│   ├── 00_logo.png                 # 插件 Logo（透明底，PNG）
│   ├── 00_bg_console.mp4           # Paper 控制台日志快速滚屏（5s）
│   ├── 00_title.mp4                # 标题动画「OrzMC — 让你的 Paper 服务器更强大」
│   ├── 01_plugins_dir.mp4          # 文件管理器：plugins/ 目录展示 OrzMC.jar（5s）
│   ├── 01_console_startup.mp4      # 服务端启动日志：插件加载成功（10s）
│   ├── 01_config_dir.mp4           # plugins/OrzMC/ 目录文件列表（5s）
│   ├── 01_feature_map.png          # 功能思维导图/大纲图
│   ├── 02_drag_jar.mp4             # 拖拽 jar 到 plugins/（5s）
│   ├── 02_server_start.mp4         # 终端启动 Paper（10s）
│   ├── 02_console_success.mp4      # 插件加载成功日志（5s）
│   ├── 02_config_yml.mp4           # 编辑器打开 config.yml 滚动展示（15s）
│   ├── 03_qq_chat.mp4              # QQ 群聊天窗口录制（含 $l $w $h $b $e 操作）
│   ├── 03_discord_chat.mp4         # Discord 频道录制
│   ├── 03_lark_chat.mp4            # 飞书群录制
│   ├── 03_bot_yml.mp4              # bot.yml 配置展示（5s）
│   ├── 03_easybot_yml.mp4          # easybot.yml 配置展示（5s）
│   ├── 04_qq_whitelist.mp4         # QQ 群 $a $r $w 操作录制
│   ├── 04_kick_screen.mp4          # 游戏画面：非白名单玩家被踢
│   ├── 04_kick_config.mp4          # config.yml 高亮 whitelist.kick_message
│   ├── 05_portal_create.mp4        # 游戏内 /portal 创建传送门
│   ├── 05_portal_teleport.mp4      # 玩家踩传送门 transfer 到目标服
│   ├── 05_portal_remove.mp4        # /portal remove 删除
│   ├── 05_portals_yml.mp4          # portals.yml 文件展示
│   ├── 06_tnt_blocked.mp4          # TNT 放置被阻止
│   ├── 06_tnt_placed.mp4           # 白名单区域 TNT 正常放置
│   ├── 06_tnt_explode.mp4          # TNT 爆炸（含告警通知）
│   ├── 07_geoip_config.mp4         # config.yml 高亮 allow_country_code
│   ├── 07_geoip_allowed.mp4        # 允许地区玩家成功进入
│   ├── 07_geoip_blocked.mp4        # 非允许地区玩家被阻止
│   ├── 07_geoip_alert.mp4          # QQ 群 GeoIP 拦截告警
│   ├── 08_tpbow_get.mp4            # /tpbow 获得传送弓
│   ├── 08_tpbow_shoot.mp4          # 射箭落地 → 传送
│   ├── 08_tpbow_safe.mp4           # 落点危险 → 就近搜索安全位置
│   ├── 09_backup_start.mp4         # QQ 群 $b 触发备份
│   ├── 09_backup_progress.mp4      # Bot 实时推送备份进度
│   ├── 09_backup_done.mp4          # 备份完成通知
│   ├── 09_optimize_run.mp4         # $o 启动地图优化
│   ├── 09_backup_dir.mp4           # 备份文件目录展示
│   ├── 10_config_list.mp4          # 游戏内 /config list
│   ├── 10_config_get.mp4           # /config get tnt.enable
│   ├── 10_config_set.mp4           # /config set tnt.enable true
│   ├── 10_config_verify.mp4        # 验证 TNT 保护生效
│   ├── 10_config_reload.mp4        # /config reload
│   ├── 11_guide_book.mp4           # 新玩家自动获得指南书（翻页）
│   ├── 11_menu.mp4                 # /menu 呼出 GUI 菜单
│   ├── 11_notification.mp4         # QQ 群收到玩家上线/下线通知
│   ├── 11_templates_yml.mp4        # templates.yml 展示
│   └── 12_outro.png                # 结束画面（Logo + 下载信息 + 二维码）
│
├── audio/
│   ├── bgm_intro.mp3               # 开场背景音乐（欢快，15s）
│   ├── bgm_main.mp3                # 主体背景音乐（轻松，8min）
│   ├── bgm_outro.mp3               # 结尾渐弱收束（30s）
│   ├── sfx_click.mp3               # 鼠标点击音效（0.5s）
│   ├── sfx_success.mp3             # 操作成功音效（1s）
│   ├── sfx_alert.mp3               # 告警提示音（1s）
│   ├── sfx_portal.mp3              # 传送门激活音效（1s）
│   ├── sfx_tnt.mp3                 # 爆炸音效（1s）
│   ├── sfx_bow.mp3                 # 拉弓射箭音效（1s）
│   └── sfx_book.mp3                # 翻书音效（0.5s）
│
└── text_overlays/                   ← 字幕/文字覆层（PNG 透明底）
    ├── subtitle_logo.png           # 频道 Logo 水印（全程右下角）
    ├── card_feature_overview.png   # 核心功能模块列表卡
    ├── card_bot_commands.png       # Bot 命令一览表
    ├── card_config_items.png       # 可配置项清单
    ├── card_download_links.png     # 下载地址汇总卡
    └── qr_qq_channel.png           # QQ 频道二维码
```

---

## 🎬 分镜制作表

每段包含：
- `素材` → 使用的源素材文件名
- `画面` → 详细镜头运动、裁切、动画说明
- `口播` → 配音文本
- `覆层` → 叠加的文字、图标、高亮标注
- `音效` → 背景音乐切换 / SFX
- `过渡` → 进出场方式

---

### 0️⃣ 开场（0:00–0:15）

| 字段 | 内容 |
|------|------|
| **素材** | `00_logo.png`、`00_bg_console.mp4`、`00_title.mp4` |
| **画面** | 黑场 0.5s → `00_bg_console.mp4` 全屏背景（50% 透明度叠加暗色遮罩）→ 1.0s 淡入 `00_logo.png` 居中（缩放动画 80%→100%, 0.6s ease-out）→ 停留 1.5s → 1.0s 淡入 `00_title.mp4` 居中偏下位置 → 定格至段落结束 |
| **口播** | "大家好，欢迎来到本期的插件介绍。今天要给大家带来的是一款功能非常全面的 Paper 服务器管理插件 —— OrzMC。它集成了多平台机器人系统、白名单管理、跨服传送门、安全防护、世界维护等一系列实用功能，让服务器管理变得前所未有的轻松。" |
| **覆层** | 右下角 `subtitle_logo.png` 全程显示（15% 透明度） |
| **音效** | `bgm_intro.mp3` 从第 0.5s 开始渐入（0→100%, 2s），全程背景 |
| **过渡** | 淡入开场 → 交叉溶解转入下一段 |

---

### 1️⃣ 插件总览（0:15–1:00）

| 字段 | 内容 |
|------|------|
| **素材** | `01_plugins_dir.mp4`、`01_console_startup.mp4`、`01_config_dir.mp4`、`01_feature_map.png` |
| **画面** | 三段素材各 8s 依次切换：① `01_plugins_dir.mp4`（光标悬停高亮 OrzMC.jar）→ ② `01_console_startup.mp4`（高亮 `[OrzMC] Loaded` 日志行，放大 115% 3s）→ ③ `01_config_dir.mp4`（依次列出配置文件）→ ④ `01_feature_map.png` 全屏展示，配合功能列表逐行划入（每行间隔 0.5s，共 12 行 6s） |
| **口播** | "OrzMC 是一款专为 PaperMC 设计的多功能服务器插件，支持的版本是最新的 Paper 1.26。它的核心亮点是打通了 QQ、Discord、飞书和 EasyBot 网关四个平台，管理员可以通过聊天群直接管理服务器。除此之外，它还涵盖了白名单管理、跨服传送门、TNT 防熊、GeoIP 地区限制、传送弓、在线世界备份与优化、运行时热配置等等十几个功能模块。目前插件已经发布在 Hangar 和 Modrinth 两个平台，完全免费使用。" |
| **覆层** | 显示 `card_feature_overview.png` 作为画中画叠加（右下角，35% 尺寸） |
| **音效** | `bgm_intro.mp3` 过渡到 `bgm_main.mp3`（交叉淡变 2s），全程背景 |
| **过渡** | 快速溶解 → 下一段 |

---

### 2️⃣ 安装与基本配置（1:00–2:00）

| 字段 | 内容 |
|------|------|
| **素材** | `02_drag_jar.mp4`、`02_server_start.mp4`、`02_console_success.mp4`、`02_config_yml.mp4` |
| **画面** | ① `02_drag_jar.mp4` 播放 8s，用红色箭头动画指示拖拽路径 → ② `02_server_start.mp4` 加速播放（3× speed），光标停在 Paper 启动命令 → ③ `02_console_success.mp4` 正常速度，日志行用黄色高亮框逐行动画标注（[OrzMC] loading... → [OrzMC] Load configs → [OrzMC] Enabled）→ ④ `02_config_yml.mp4` 滚动展示，关键字段 `force_whitelist`、`tnt.enable`、`geoip.allow_country_code` 用左侧标注线+文字高亮（每个字段停留 2s） |
| **口播** | "安装非常简单，跟其他 Paper 插件一样，把下载好的 Jar 文件放到服务器的 plugins 目录，启动一次服务端，插件就会自动生成配置目录。我们先停掉服务器，来看一下主要的配置文件 config.yml。这里面可以控制白名单开关、TNT 保护、GeoIP 地区限制、地图维护参数等等。这些选项都可以在游戏内通过 /config 命令动态修改，我们后面会演示。现在我们配置好之后，重启服务器让配置生效。" |
| **覆层** | 同步高亮文字：左侧标注线 + 配置项名 + 当前值（白色文字，黄色高亮底） |
| **音效** | `bgm_main.mp3` 继续；`sfx_click.mp3` 在每个箭头/标注出现时触发 |
| **过渡** | 滑动切入 → 下一段 |

---

### 3️⃣ 多平台 Bot 系统（2:00–3:30）

| 字段 | 内容 |
|------|------|
| **素材** | `03_qq_chat.mp4`、`03_discord_chat.mp4`、`03_lark_chat.mp4`、`03_bot_yml.mp4`、`03_easybot_yml.mp4` |
| **画面** | **三分屏布局**（持续 50s）：左 1/3 QQ `03_qq_chat.mp4`、中 1/3 Discord `03_discord_chat.mp4`、右 1/3 飞书 `03_lark_chat.mp4`。操作同步演示 5 个命令，每个命令叠加文字说明条（顶栏，黑底白字，1.5s 停留）→ 在第 5 个命令完成后切为全屏 `03_bot_yml.mp4`（8s）→ 再切 `03_easybot_yml.mp4`（10s），高亮 `admin_group`、`player_group`、`admin_dm` 三个字段 |
| **口播** | "OrzMC 最亮眼的功能就是这个多平台 Bot 系统。插件启动后，你可以在 QQ 群、Discord 频道或者飞书群里直接向服务器发送命令，像这样：输入 `$l` 就能查看当前在线玩家，包括所在世界、精确坐标和游戏角色信息。输入 `$w` 可以查看白名单列表，支持分页显示。管理员还可以用 `$a` 和 `$r` 来添加或移除白名单玩家。`$b` 是一键备份世界，`$o` 是优化地图文件大小，`$e` 可以直接执行任意控制台命令。如果你忘了某个命令怎么用，在命令后面加空格加问号，比如 `$a ?`，Bot 会返回详细的用法提示。在配置方面，你可以在 bot.yml 里设置默认的命令前缀字符。如果使用 EasyBot 网关，还可以在每个平台上独立配置管理群、玩家群和管理员私聊的目标 ID，实现精细化的消息路由。更重要的是，服务器里发生的事件 —— 比如玩家上线、下线、被踢出、TNT 爆炸 —— 都会自动推送到相应的群聊，管理员可以第一时间掌握服务器动态。" |
| **覆层** | ① 三分屏上叠 `card_bot_commands.png`（左下 30% 透明浮层，持续 30s）→ ② 命令说明条：「`$l` — 查看在线玩家」等逐一替换 → ③ 配置画面中高亮标注字段名 |
| **音效** | `bgm_main.mp3`；`sfx_success.mp3` 在每个 Bot 回复出现时触发 |
| **过渡** | 三分屏推入 → 配置画面 dissolve → 下一段 |

---

### 4️⃣ 白名单管理（3:30–4:30）

| 字段 | 内容 |
|------|------|
| **素材** | `04_qq_whitelist.mp4`、`04_kick_screen.mp4`、`04_kick_config.mp4` |
| **画面** | ① `04_qq_whitelist.mp4` 全屏播放（25s），依次展示：`$a Alice Bob` 输入（光标闪烁 1s）→ Bot 回复「已添加 2 名玩家」（高亮框 2s）→ `$r Charlie` → Bot 回复移除成功 → `$w` → 更新后白名单列表滚动 → ② `04_kick_screen.mp4`（15s）, 非白名单玩家尝试加入，踢出画面定格 3s，高亮自定义提示文字 → ③ `04_kick_config.mp4` 展示配置段，逐行高亮 `title`、`platform`、`name` |
| **口播** | "白名单是很多服务器的基础功能，OrzMC 把它做得更完善。管理员在群里输入 `$a` 后面跟上玩家名，就能把玩家加到白名单，`$r` 则是移除。而且，插件会自动清理超过 90 天未上线的非活跃白名单玩家，保持名单整洁。当非白名单玩家尝试加入服务器时，会被踢出并看到一张自定义的提示页面。你可以在这里配置标题、引导文案，甚至还可以放上你的 B站、YouTube 频道链接。这个踢出消息的文案完全可以在 config.yml 的 whitelist.kick_message 里面自由定制。" |
| **覆层** | Bot 消息区域加发光高亮圈动画（脉动 2s）；配置字段用黄色高亮+左侧标注线 |
| **音效** | `bgm_main.mp3` 继续 |
| **过渡** | 滑动 → 下一段 |

---

### 5️⃣ 跨服传送门（4:30–5:15）

| 字段 | 内容 |
|------|------|
| **素材** | `05_portal_create.mp4`、`05_portal_teleport.mp4`、`05_portal_remove.mp4`、`05_portals_yml.mp4` |
| **画面** | ① `05_portal_create.mp4` 正常速度播放（12s），展示下界合金传送门自动搭建动画，上方文字标签「mc.example.com:25565」用箭头指向 → ② `05_portal_teleport.mp4` 玩家走进传送门→画面淡入到目标服务器（10s）→ ③ `05_portal_remove.mp4`（8s）`/portal remove` 操作，传送门消失 → ④ `05_portals_yml.mp4`（5s）文件内容展示 |
| **口播** | "如果你运营的是 BungeeCord 或 Velocity 集群，跨服传送门就会很实用。管理员只需要站在想放传送门的位置，输入 /portal 后面跟上目标服务器的地址和端口号，插件会在你脚下自动搭建一个 4x5 的下界传送门框架。玩家走进去就会被跨服传送到目标服务器。如果不再需要，用 /portal remove 加地址就能一键移除。所有传送门信息都存储在 portals.yml 中，支持运行时修改即生效。" |
| **覆层** | 传送门标签文字框+箭头标注；服务器地址用等宽字体高亮 |
| **音效** | `sfx_portal.mp3` 在传送瞬间触发 |
| **过渡** | 溶解 → 下一段 |

---

### 6️⃣ TNT 保护（5:15–6:00）

| 字段 | 内容 |
|------|------|
| **素材** | `06_tnt_blocked.mp4`、`06_tnt_placed.mp4`、`06_tnt_explode.mp4` |
| **画面** | ① `06_tnt_blocked.mp4`（10s）放置失败动作循环播放 2 次，红色 X 动画覆盖 → ② `06_tnt_placed.mp4`（8s）白名单区域允许放置，绿色 ✓ 动画覆盖 → ③ `06_tnt_explode.mp4`（15s）爆炸慢动作回放（50% speed），爆炸瞬间切换到 QQ 群告警通知，告警消息用红色高亮框 |
| **口播** | "TNT 防熊是很多生存服务器的刚需。OrzMC 的 TNT 保护模块可以做到：第一，限制 TNT 的放置范围 —— 你可以在配置文件中定义允许放置 TNT 的区域白名单，白名单之外的地方放不了 TNT。第二，一旦有 TNT 被点燃或爆炸，会立刻通过 Bot 通知到管理群，包含具体的世界、坐标、触发者和方块类型。第三，可以单独禁止重生锚的爆炸行为。另外还有放置冷却限制，防止玩家频繁放置 TNT 捣乱。" |
| **覆层** | 红色 X / 绿色 ✓ 动画叠加（0.3s scale-in）；告警消息用红色外发光框 |
| **音效** | `sfx_tnt.mp3` 在爆炸瞬间触发，低音增强；高亮框出现时 `sfx_alert.mp3` |
| **过渡** | 快速切场 → 下一段 |

---

### 7️⃣ GeoIP 安全控制（6:00–6:40）

| 字段 | 内容 |
|------|------|
| **素材** | `07_geoip_config.mp4`、`07_geoip_allowed.mp4`、`07_geoip_blocked.mp4`、`07_geoip_alert.mp4` |
| **画面** | ① `07_geoip_config.mp4`（8s）高亮 `geoip.allow_country_code: [CN]` 和 `geoip.allow_country_code: []` 前后对比 → ② `07_geoip_allowed.mp4`（8s）正常进入，右上角显示国家旗标+「✓ CN」→ ③ `07_geoip_blocked.mp4`（10s）连接被拒，红色旗标+「✗ US」→ ④ `07_geoip_alert.mp4`（8s）QQ 群告警，消息中含玩家名、IP、国家码、允许列表 |
| **口播** | "GeoIP 模块让你可以根据玩家的 IP 地理位置来控制服务器访问。比如说你的服务器主要面向国内玩家，可以在 config.yml 里设置 allow_country_code: [CN]，只有中国地区的 IP 才能进入。被阻止的玩家和群聊管理员都会收到详细的告警信息，包含该玩家的 IP、所在国家代码和当前的允许列表。这个功能对于防止恶意攻击、做区域性运营都非常有用。" |
| **覆层** | 国家旗标 + ✓/✗ 动画；IP 地址等宽字体高亮 |
| **音效** | `sfx_alert.mp3` 在阻断/告警时触发 |
| **过渡** | 溶解 → 下一段 |

---

### 8️⃣ 传送弓（6:40–7:20）

| 字段 | 内容 |
|------|------|
| **素材** | `08_tpbow_get.mp4`、`08_tpbow_shoot.mp4`、`08_tpbow_safe.mp4` |
| **画面** | ① `08_tpbow_get.mp4`（8s）输入 `/tpbow` → 物品栏出现附魔弓特写（放大 150%，旋转 360° 展示 2s）→ ② `08_tpbow_shoot.mp4`（12s）拉弓→射箭→慢动作追踪箭矢轨迹→落地→传送（箭矢路径用运动轨迹虚线标注）→ ③ `08_tpbow_safe.mp4`（15s）箭落岩浆→落点标记为红色危险→自动搜索绿色安全点→传送 |
| **口播** | "传送弓是一个有趣的玩法功能。输入 /tpbow 或者 /tpb，你就能获得一把带有无限附魔的传送弓。射出箭之后，当箭落地，你会被瞬间传送到箭的落点位置。它内置了安全检测机制：如果落点不安全 —— 比如掉进岩浆或者虚空 —— 插件会自动在附近寻找一个安全的固体方块位置，让你安全着陆。这个功能在跑图、探险的时候非常方便。" |
| **覆层** | 箭矢轨迹虚线动画；绿色安全点/红色危险点标记 |
| **音效** | `sfx_bow.mp3` 在拉弓时触发；传送时 `sfx_portal.mp3`（轻快版本） |
| **过渡** | 快速溶解 → 下一段 |

---

### 9️⃣ 世界维护 — 备份与优化（7:20–8:10）

| 字段 | 内容 |
|------|------|
| **素材** | `09_backup_start.mp4`、`09_backup_progress.mp4`、`09_backup_done.mp4`、`09_optimize_run.mp4`、`09_backup_dir.mp4` |
| **画面** | ① `09_backup_start.mp4`（6s）QQ 群输入 `$b` → Bot 回复「开始备份...」→ ② `09_backup_progress.mp4`（15s）进度条动画：阶段名、百分比、速率、ETA 数据逐帧变化 → ③ `09_backup_done.mp4`（5s）「备份完成，耗时 xx 秒」→ ④ `09_optimize_run.mp4`（10s）类似进度展示，地图优化前后文件大小对比（柱状图动画）→ ⑤ `09_backup_dir.mp4`（4s）目录文件列表 |
| **口播** | "世界维护是每一位服主的日常功课。OrzMC 允许你在服务器运行时在线发起世界备份和地图优化。管理员在群里输入 `$b`，插件就会开始逐世界备份，并通过 Bot 实时推送进度 —— 包括当前阶段、完成百分比、处理速率和预计剩余时间。`$o` 命令则是执行地图文件优化，可以显著减小世界文件体积，提升服务器性能。备份保留数量可以在配置里自定义，默认保留最近 5 份。维护期间，服务器的 MOTD 会自动切换为维护提示，等完成后自动恢复。" |
| **覆层** | 进度条浮层（屏幕下方，磨砂玻璃风格）；柱状图对比动画 |
| **音效** | `bgm_main.mp3` 继续；备份完成时 `sfx_success.mp3` |
| **过渡** | 溶解 → 下一段 |

---

### 🔟 运行时配置管理（8:10–8:55）

| 字段 | 内容 |
|------|------|
| **素材** | `10_config_list.mp4`、`10_config_get.mp4`、`10_config_set.mp4`、`10_config_verify.mp4`、`10_config_reload.mp4` |
| **画面** | 五段连续操作不切场，每段 8-10s，用操作提示文字分隔：① 输入 `/config list` → 配置项滚屏 ② `/config get tnt.enable` → 显示当前值「false」③ `/config set tnt.enable true` → Bot 回复「已更新」④ `/config reload` ⑤ 验证：放置 TNT → 被阻止（呼应 6️⃣，形成闭环） |
| **口播** | "大多数插件的配置改完后要重启服务器才能生效，但 OrzMC 不需要。/config 命令支持六个子操作：list 列出所有配置项，get 查看某个配置的当前值，set 修改配置，reset 恢复默认值，dump 转储所有配置，reload 从文件重新加载。修改后立竿见影，不用停机。你可以在游戏里随时调优服务器的各项参数，非常灵活。" |
| **覆层** | 每个命令在行首叠加键盘快捷键风格标签（如 `[ /config set ]`） |
| **音效** | `sfx_click.mp3` 每个命令输入时触发；`sfx_success.mp3` 验证成功时 |
| **过渡** | 连续操作，无过渡 → 最后 dissolve 到下一段 |

---

### 1️⃣1️⃣ 其他亮点快速展示（8:55–9:25）

| 字段 | 内容 |
|------|------|
| **素材** | `11_guide_book.mp4`、`11_menu.mp4`、`11_notification.mp4`、`11_templates_yml.mp4` |
| **画面** | **四宫格快切**，每段 7s：① 左上 `11_guide_book.mp4` 指南书翻页动画 ② 右上 `11_menu.mp4` GUI 菜单 ③ 左下 `11_notification.mp4` QQ 群玩家进出通知 ④ 右下 `11_templates_yml.mp4` 模板配置滚动。每 2s 切换一个宫格为全屏焦点（顺时针循环 2 轮） |
| **口播** | "还有一些贴心的小功能：新玩家第一次进服会自动获得一本指南书，内容你可以通过 YAML 自定义，支持文字样式和超链接。/menu 命令提供游戏内的功能快捷入口。所有推送到 Bot 的消息 —— 无论是玩家上下线通知、TNT 告警还是异常提醒 —— 都可以在单独的模板配置文件中自定义文字格式，想怎么改就怎么改。" |
| **覆层** | 宫格分割线（半透明白色 1px）；焦点宫格使用发光边框 |
| **音效** | `bgm_main.mp3` 保持；`sfx_book.mp3` 指南书翻页时触发 |
| **过渡** | 快速切场，全片节奏最密集段落 → 渐慢过渡到结尾 |

---

### 1️⃣2️⃣ 结束（9:25–10:00）

| 字段 | 内容 |
|------|------|
| **素材** | `12_outro.png` |
| **画面** | `12_outro.png` 全屏（35s），信息分层依次淡入：① Logo 和标题「OrzMC」（1.5s）→ ② 下载地址三行依次显示（每行间隔 0.8s，左对齐排列）→ ③ QQ 频道二维码从右侧滑入（2s）→ ④ 「感谢观看」字幕从底部升入（2s）→ 定格 5s → 整体淡出到黑场 |
| **口播** | "到这里，OrzMC 的主要功能就都介绍完了。这款插件完全免费，你可以在 Hangar、Modrinth 或者插件官网上找到最新版本。如果你在使用过程中遇到问题，或者有新的功能建议，欢迎到 GitHub 提交 Issue，也可以加入我们的 QQ 频道一起交流。如果本期视频对你有帮助，别忘了点赞、投币、收藏，我们下期见！" |
| **覆层** | 下载地址使用等宽字体 + 小图标（Hangar/Modrinth/Globe）；二维码加外发光 |
| **音效** | `bgm_main.mp3` 渐弱→ `bgm_outro.mp3` 交叉淡变（3s）→ 片尾结束 fade out（2s） |
| **过渡** | 整体淡出至黑场 → 黑场 2s → 全片结束 |

---

## 🎵 音频时间线

| 时间 | 音轨 | 操作 | 音量 |
|------|------|------|:----:|
| 0:00–0:15 | `bgm_intro.mp3` | 渐入（0→100%, 2s） | 15% |
| 0:15–0:17 | → `bgm_main.mp3` | 交叉淡变 | 15%→18% |
| 0:17–9:25 | `bgm_main.mp3` | 常态背景 | 18% |
| 9:25–9:28 | → `bgm_outro.mp3` | 交叉淡变 | 18%→15%→0 |
| 9:28–10:00 | `bgm_outro.mp3` | 渐弱至静音 | 15%→0 |

| 时间 | SFX | 触发点 |
|------|-----|--------|
| 1:00~2:00 | `sfx_click.mp3` | 每个箭头/标注出现时 |
| 2:00~3:30 | `sfx_success.mp3` | 每个 Bot 回复出现时 |
| 4:30~5:15 | `sfx_portal.mp3` | 传送瞬间 |
| 5:15~6:00 | `sfx_tnt.mp3`、`sfx_alert.mp3` | 爆炸瞬间、告警出现时 |
| 6:00~6:40 | `sfx_alert.mp3` | 阻断/告警时 |
| 6:40~7:20 | `sfx_bow.mp3`、`sfx_portal.mp3` | 拉弓、传送时 |
| 7:20~8:10 | `sfx_success.mp3` | 备份完成时 |
| 8:10~8:55 | `sfx_click.mp3`、`sfx_success.mp3` | 命令输入、验证成功时 |
| 8:55~9:25 | `sfx_book.mp3` | 指南书翻页时 |

---

## ⏱ 精确时间轴

```
 0:00 ┤ 开场 ────────────────────── 0:15
 0:15 ┤ 插件总览 ────────────────── 1:00
 1:00 ┤ 安装与配置 ──────────────── 2:00
 2:00 ┤ 多平台 Bot 系统 ──────────── 3:30
 3:30 ┤ 白名单管理 ──────────────── 4:30
 4:30 ┤ 跨服传送门 ──────────────── 5:15
 5:15 ┤ TNT 保护 ───────────────── 6:00
 6:00 ┤ GeoIP 安全 ──────────────── 6:40
 6:40 ┤ 传送弓 ──────────────────── 7:20
 7:20 ┤ 世界维护 ────────────────── 8:10
 8:10 ┤ 运行时配置 ──────────────── 8:55
 8:55 ┤ 其他亮点 ────────────────── 9:25
 9:25 ┤ 结束 ───────────────────── 10:00
```

---

## 🎨 视觉风格规范

| 项目 | 值 |
|------|-----|
| **主色调** | `#4FC3F7`（浅蓝）/ `#0288D1`（深蓝） |
| **强调色** | `#FFD54F`（琥珀黄，用于高亮） |
| **警告色** | `#EF5350`（红色，用于 TNT/GeoIP 阻断） |
| **成功色** | `#66BB6A`（绿色，用于操作成功） |
| **字体** | 字幕：Noto Sans SC（思源黑体）；代码：JetBrains Mono |
| **圆角** | UI 元素圆角 8px |
| **高亮风格** | 黄色半透明底色 + 左侧 3px 实线标注条 |
| **信息卡风格** | 磨砂玻璃毛玻璃（backdrop-filter: blur(12px)），白色 60% 透明度底 |

---

## 📐 分屏 / 画中画布局规格

```
三分屏（3️⃣ 多平台 Bot 系统）:
┌──────────┬──────────┬──────────┐
│  QQ 群   │ Discord  │  飞书群   │
│  33.3%   │  33.3%   │  33.3%   │
│  left:0  │ left:33.3│ left:66.6│
└──────────┴──────────┴──────────┘
顶栏：命令说明文字（黑底白字，高度 48px）

四宫格（1️⃣1️⃣ 其他亮点）:
┌──────────┬──────────┐
│ 指南书   │  GUI菜单  │
├──────────┼──────────┤
│ 通知消息  │ 模板配置  │
└──────────┴──────────┘
焦点切换：顺时针，每 2s 切换，发光边框 2px #4FC3F7
```

---

## 📄 待补素材与录制注意事项

- **服务器环境**：Paper 1.26，搭载 OrzMC 1.0.13+，预置至少 2 个测试玩家账号
- **跨服环境**：需 Velocity/BungeeCord 连接 2 个 Paper 实例
- **Bot 环境**：QQ 群（NapCatQQ 在线）、Discord 服务器（Bot Token 有效）、飞书群（Webhook 有效）
- **敏感信息处理**：录制后需用马赛克遮盖所有 IP 地址、Token、QQ 号
- **鼠标光标**：录制时放大光标（150%），使用高亮色光晕
- **字幕**：所有命令输入和 Bot 回复需叠加屏幕文字说明（左下角，半透明底）

---

*文档版本: 2.0 | 2026-07-09*
